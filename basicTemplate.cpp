#include <iostream>

// Uncomment the line underneath if doing a problem on the actual platform USACO
// since regular USACO (not USACO.guide) uses text files replace the name bcount
// with whatever the specific problem wants for file names
// #define usaco 

#ifdef usaco
std::ifstream cin("bcount.in");
std::ofstream cout("bcount.out");
#endif

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    // write solution
}
