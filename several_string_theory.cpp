#include <iostream>
#include <vector>
#include <ios>

using namespace std;
using vi = vector<int>;

void solve() {
    int n;
    // getting the number of test cases
    cin >> n;
    // iterating through the tests
    for (int i = 0; i < n; i++) {

    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    solve();
}

